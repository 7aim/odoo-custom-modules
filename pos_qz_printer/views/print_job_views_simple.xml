<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Sadə Print Job List View -->
    <record id="view_print_job_list_simple" model="ir.ui.view">
        <field name="name">pos.print.job.list.simple</field>
        <field name="model">pos.print.job</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="status"/>
                <field name="create_date"/>
            </list>
        </field>
    </record>

    <!-- Sadə Print Job Form View -->
    <record id="view_print_job_form_simple" model="ir.ui.view">
        <field name="name">pos.print.job.form.simple</field>
        <field name="model">pos.print.job</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_print_browser" string="🖨️ Browser-də Çap Et" type="object" class="oe_highlight"/>
                    <button name="action_mark_printed" string="✅ Çap Edilmiş" type="object" invisible="status == 'printed'"/>
                    <field name="status" widget="statusbar" statusbar_visible="pending,printed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <field name="user_id"/>
                        <field name="create_date"/>
                    </group>

                    <notebook>
                        <page string="Məzmun">
                            <field name="content" widget="text"/>
                        </page>
                        <page string="Önizləmə">
                            <div class="text-center">
                                <h4>Çap Önizləməsi</h4>
                                <div style="border: 1px solid #ddd; padding: 20px; background: #f9f9f9; max-width: 600px; margin: 0 auto;">
                                    <field name="preview_html" widget="html"/>
                                </div>
                            </div>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Print Job Action -->
    <record id="action_print_job_simple" model="ir.actions.act_window">
        <field name="name">Çap İşləri</field>
        <field name="res_model">pos.print.job</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_print_job_list_simple"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Hələ çap işi yoxdur!
            </p>
            <p>
                Çap işləri avtomatik olaraq yaradılır və burada idarə olunur.
            </p>
        </field>
    </record>

    <!-- Menu Items -->
    <menuitem
        id="menu_print_job_simple"
        name="Çap İşləri"
        parent="qz_printer_main_menu"
        action="action_print_job_simple"
        sequence="20"/>

</odoo>
